---
title: Page de CV français
---


#cv

  %div.row
    %div.3u{class: "12u$(mobile)"}

    %div.9u{class: "12u$(mobile)"}

      %h2 Expériences

      - jobs_sorted.each do |key, job|
        - title = localized(job.title)
        - year, name = key.split('_')

        .chapter.job
          / - unless job.respond_to? :title
          /   %h3= "SKIPPED [#{key}]"
          /   - next

          %h3= "#{title} [#{year}] [#{name}]"

          %p= job.keywords.join(' - ') if job.keywords.is_a? Array

          .sidenotes
            = image_tag("logos/#{name}.png", class: "logo", title: job.location)
            .place
              = localized job.place
            .where
              = job.location
              = " - "
              = year
              = " - "
              = localized job.duration
       
          %ul
            - if job.what.is_a?(Array)
              - job.what.each do |w|
                %li
                  %h4= localized w.title
                  .small= localized w.detail

  


